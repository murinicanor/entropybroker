- client: in classes
- retrieve from other eb instance (eb_proxy)
- keep stats of disk-get & put to advise user of ideal number of memory pools
- acls (ip)
- http://en.wikipedia.org/wiki/Secure_Remote_Password_protocol

- scsi-ping-entropyd

http://www.cryptopp.com/
http://en.wikipedia.org/wiki/Twofish <--

- filter.cpp:
  - whitening 0/0 en 1/1 -> drop, 01=0bit, 10=1bit
  - what else? xoren met prng van openssl?

- multiple hosts
  ^ load balance 'server' but also client

- fips140 & scc in een wrapper 'verify_entropy'-class proppen

- abstraced prng to objects

- error_exit laat niks zien

- servers: work in units given by broker

- random: in object en niet meer met global lock

- merge optimizen:
  - get list of bit counts
  - sort van klein naar groot
  - dan door lijst en dan steeds 2 of meer combineren
    int addtoindex = -1;
    while(nr < aantal)
    {
	if (index == -1) {
		addtoindex = id[nr++];
		if (nr == aantal)
			break;
	}
	
	int addfromindex = id[nr++];

	add addfromindex to addtoindex;
	if (addtoindex == full)
		addtoindex = -1;
    }

SVN: $Revision$
